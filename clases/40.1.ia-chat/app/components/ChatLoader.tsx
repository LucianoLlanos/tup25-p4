/**
 * ChatLoader - Indicador de carga
 * Muestra un indicador visual mientras el asistente está procesando
 */

import { Loader } from '@/components/ai-elements/loader';

export const ChatLoader = () => {
  return <Loader />;
};
